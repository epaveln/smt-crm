{% extends 'base.html.twig' %}

{% block title %}Измение тэга{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-sm-9 col-md-7 col-lg-12 mx-auto">
            <div class="card card-signin my-5">
                <div class="card-body">
                    <h5 class="card-title text-center">Измение тэга</h5>
                    {{ form_start(editTagForm) }}
                    {{ form_row(editTagForm.name, {label: "Название страны."}) }}
                    <button class="btn btn-lg btn-primary" type = "submit">Изменить</button>
                    {{ form_end(editTagForm) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
